# Limitations — LEASH

Honest scope boundary: what is built vs what is deliberately gated. Every gated item names its reason.

> **NOTE (2026-09-13, UI redesign).** The frontend was redesigned (Signal Grid visual system + "Tether" logo + owner-first multi-page IA). This was presentation, information architecture, positioning, and logo ONLY. NO limitation below changed: the in-scope set, the feature-gated stretches, and every stated reason are UNCHANGED. The enforcement behavior, the frozen floor, and the honesty locks are untouched.

## In scope (built + demonstrable for this submission)
- ENSv2 Sepolia 3-level hierarchy (root → org → agent) with EAC role grant/revoke and Permissioned-Resolver `leash.policy` text records read as live settlement policy.
- Self-hosted Hedera x402 facilitator: reads ENS pre-settlement, enforces cap + allowlist + binding (BigInt raw units), settles native gas-free (fee-payer), logs ALLOW/DENY to HCS. Fail-closed on RPC/malformed/revoked/replay.
- Real paid request end-to-end on Hedera testnet with own HTS USDC; HashScan-verifiable.
- Privy P-256-owner treasury wallet + funding policy (cap + allowlist) + live leaked-key over-fund DENY.
- **Funding-allowlist reconcile (WS7 A3, was the DEV-030 caveat, now IN SCOPE):** on agent register the new agent's EVM-facade address is added to the Privy funding-policy allowlist, so a newly-registered console agent can be funded in-cap (real transfer) while an over-fund still DENIES `FUNDING_DENIED`. The register-time allowlist gap is closed; funding is no longer limited to pre-seeded agents. (F-018)
- **Co-hold identity model (WS7 C1):** in `/app`, the signed-in user co-holds the kill-switch EAC role for their own agents; the relayer is a delegated operator (gasless ops), not the sole custodian. The user has real on-chain revoke authority (INVARIANT #12, F-023). This is the `/app` identity model, distinct from the `/demo` sandbox (server-orchestrated, no login).
- **Console controls (WS7 B1/B2):** allowlist edit (rewrites `leash.policy` on-chain) and un-revoke / re-activate (setPolicy rebind + DB status) in `/app`, both authz-guarded. (F-020, F-021)
- **Live spend feed (WS7 B3):** `/app` per-agent + org-level feed indexed from HCS (name/decision/amount/reason/ts). Index layer, off enforcement. (F-022)
- **ENS agent-identity records (WS7 D1):** advisory agent-identity text records + reverse name per agent child, surfaced in UI/`/proof`; never an enforcement input (INVARIANT #13). (F-024)
- **Console authz (WS7 A1) + rate limiting (WS7 A4) + durable replay store (WS7 A5):** every `/app` route verifies the Privy auth token (INVARIANT #11); demo + mutation routes are rate-limited (`429`); the replay `seen` set is Neon-backed so `REPLAY` survives a facilitator restart (INVARIANT #14).
- Judge sandbox (`/demo`, zero-setup, server-orchestrated) driving the full hero flow: grant → spend → refuse → revoke (fail-closed) → Privy DENY.
- Revocation as one on-chain write (clear record OR revoke EAC role) with the next payment failing closed.

## Feature-gated (stretch, not built this scope)
- **On-chain (contract-level) enforcement of the cap** (Hedera contract wallet + on-chain cap mirror). Reason: multi-day build; out of the window. Today enforcement is facilitator-trusted; contract-level enforcement is roadmap. (D-1)
- **D2 - Hedera extras** (HCS-14 agent identity, x402 metering / aggregate billing, Scheduled Transactions). Reason: beyond the current WS-7 scope (Correctness + controls + D1); held as stretch. The x402 leg is already covered via the live paid request + HCS audit (F-001/F-014); D2 adds no new capability gate.
- **D3 - Privy 2nd control** (a co-signer/approver control beyond the funding policy). Reason: INVARIANT-protective to omit - an active approver re-opens the thesis drift E-2 was dropped for (INVARIANT #6 / concern #4 [C]). The Privy funding rail stands on ONE control; held as stretch, not needed.
- **Instant revocation on the advisory pre-screen** — the `/app` (non-demo) pre-screen may be ≤30s stale. Reason: latency; the authoritative settle-time read is always immediate and no-cache. (INVARIANT #3, disclosed)
- **Public agent-name marketplace / discovery.** Reason: out of thesis scope (explicit warroom out-of-scope).
- **Mainnet deployment / real funds.** Reason: ENSv2 runs on Sepolia; Hedera uses testnet; no real value at risk by design.
- **Governance for other chains (Arc, Base, other EVM networks).** Reason: spend governance (in-cap allow / over-cap refuse / revoke) runs on Hedera today because the co-signing facilitator LEASH runs is the x402/Hedera one; native governance on another chain needs a co-signer plus policy read deployed per chain. Identity binding is already multi-chain (any EVM address / ERC-8004, on-chain-resolved) and the Privy funding mechanism is EVM-general and portable. Roadmap: in progress.
- **Aggregate/velocity spend limits beyond `fundingCap`.** Reason: per-call cap is `maxPerCall`; the aggregate bound is `fundingCap` on the funding rail. No per-second rate limit on in-cap payments (honest scope, INVARIANT #9).
- **[SKILL] REFRAME — Separate-trust-domain co-signer service.** The 2-of-2 spend co-signer key (`LEASH_COSIGNER_KEY`) runs in the SAME process and trust-domain as the operator. Reason: a co-signer isolated on a separate host / HSM / trust boundary is a hardening step out of the window. Roadmap. (See caveat below.)
- **[SKILL] REFRAME — Proof-of-control on the ERC-8004 external identity.** Register resolves the ERC-8004 owner via `ownerOf` (on-chain-resolved) but does not require the registrant to sign a challenge proving they control the address. Reason: a signed proof-of-control handshake is added scope. Roadmap. (See caveat below.)

## Known honest caveats (repeat at demo-rehearsal)
- Enforcement is facilitator-trusted (Trustlessness = FALSE). An agent with funds could pay via a different facilitator or a direct transfer; the facilitator LEASH runs enforces the org's ENS-declared policy for the org's own facilitator (capital-out) plus the Privy funding rail (capital-in). The org-wide kill is funding-rail control plus record/role revocation.
- ENSv2 Sepolia is alpha; the 3-level `getEnsText` read path is the least-tested surface (WS-1 read-back proves it).

### REFRAME caveats (build-delta, 2026-09-12) [SKILL]
- **Rolling daily/weekly caps are a SOFT budget, not a hard bound.** The rolling total is read from the HCS topic via the Hedera Mirror Node, which is a LAGGING index. So the rolling cap can be briefly exceeded: worst-case overspend under C concurrent in-flight payments is about C × `maxPerCall` (NOT 1 × `maxPerCall`), because each concurrent settle can read a total that predates the others. This is disclosed, not hidden. If a tighter bound is wanted, per-agent settle can be serialized (roadmap). The hard guarantees remain `maxPerCall` (per-call, live ENS) and `fundingCap` (aggregate, Privy funding rail).
- **The spend co-signer runs in the operator's own process/trust-domain.** `LEASH_COSIGNER_KEY` is a distinct key from `HEDERA_OPERATOR_KEY` (the gas fee-payer) and this is asserted at startup, so "authority key ≠ gas key" is literally true within the process — but both keys live on the same host in the same process. A co-signer isolated on a separate trust domain (separate host/HSM) is roadmap. The honest claim today is co-control (LEASH's co-signature is required and it refuses over-policy), not a physically separated second signer.
- **The ERC-8004 external identity is on-chain-resolved, not proof-of-control.** `ownerOf` returns the owner recorded on the canonical ERC-8004 Identity Registry; it does NOT prove that the party registering with LEASH controls that address. The badge reads "on-chain-resolved", never "verified". A signed proof-of-control handshake is roadmap.
- **Funding targets the spending account's long-zero EVM address.** The 2-of-2 KeyList spending account has no key-derived EVM alias, so Privy/treasury funding is sent to the account's long-zero EVM address (its account-id-derived EVM form). If long-zero HTS funding fails live, the Privy over-fund DENY beat is demoed against the treasury source instead (which still exercises the Privy funding rail); the DENY behavior is unchanged, only the funding target moves.
