# FEATURE-OBSERVABLES — LEASH

One verifiable observable per P0/P1 feature: proves the feature WORKS, not just that it exists. Sentinel-fail strings are the "fabricated/fallback" markers stress_test greps for.

| feature_id | feature | observable | test_command | sentinel_fail | verified_by |
|---|---|---|---|---|---|
| F-001 | Agent pays within ENS-encoded cap, gas-free | a real Hedera settle tx exists on HashScan AND the agent account paid 0 gas (facilitator is fee-payer) | run `agent/pay.ts` in-cap (3 USDC) then `curl mirrornode .../transactions/{id}` → `charged_tx_fee` paid by operator, transfers show USDC moved | settle tx absent OR agent charged the fee | PLAN Task 3.1 (DP-3) |
| F-002 | Over-cap payment refused at the rail | over-cap (50 USDC) returns abort reason `OVER_CAP` and NO settle tx appears | run `agent/pay.ts` amount 50_000_000 → response reason == "OVER_CAP"; HashScan shows no matching settle | reason != OVER_CAP OR a settle tx appears | PLAN Task 3.2 |
| F-003 | Revoke kills spend in one on-chain write (fail-closed) | after a single `clearPolicy`/`revokeRoles` Sepolia tx, the next identical in-cap call returns `REVOKED` | run revoke tx, then re-run the F-001 in-cap call → reason == "REVOKED"; ENS `text(node,'leash.policy')` == "" | in-cap call still settles after revoke | PLAN Task 3.3 (VM-1) |
| F-004 | Fail-closed on RPC failure (never fail-open) | with the Sepolia RPC unreachable, a previously-working in-cap call returns `RPC_ERROR`, never settles | point SEPOLIA_RPC_URL at a dead endpoint, run in-cap call → reason == "RPC_ERROR"; no settle | call settles when ENS unreadable | INVARIANTS #1; PLAN Task 2.1 |
| F-005 | TOCTOU-closed: in-flight revoke fails the settle | revoke landing between verify and settle → settle-time no-cache read returns `REVOKED`, no submit | `npm run test -- toctou` → REVOKED at settle; no Hedera submit | in-flight payment settles after mid-flight revoke | INVARIANTS #2; PLAN Task 2.3 step 3 |
| F-006 | Binding anti-spoof | a payment whose payer != record.hederaAccount returns `BINDING_MISMATCH` | send a payment with X-Leash-Agent naming a high-cap agent but paying from another account → reason == "BINDING_MISMATCH" | mismatched-payer payment settles | INVARIANTS #8; PLAN Task 2.3 |
| F-007 | Raw-unit BigInt cap; malformed policy fails closed | amount==cap settles; amount==cap+1 → OVER_CAP; non-numeric maxPerCall → MALFORMED_POLICY | `npm run test -- authorize` boundary + malformed cases pass | off-by-one settles OR garbage cap settles | INVARIANTS #7; PLAN Task 2.1 |
| F-008 | Replay rejected (per-call not aggregate-only) | re-submitting a settled X-PAYMENT payload returns `REPLAY`; no second settle | replay the captured F-001 payload → reason == "REPLAY" (or Hedera DUPLICATE_TRANSACTION); no 2nd settle | replayed payload settles again | INVARIANTS #9; PLAN Task 3.2 step 2 |
| F-009 | Privy leaked-key over-fund DENY (P-256 owner enforced) | an over-`fundingCap` transfer from the OWNED treasury returns `FUNDING_DENIED` BEFORE broadcast | `npm run test -- privy` over-cap case → result.denied == true, reason "FUNDING_DENIED"; no broadcast tx | over-fund broadcasts (owner-less fail-open) | INVARIANTS #5; PLAN Task 0.2 (DP-0) + 4.1 |
| F-010 | Privy is a funding-rail policy, not a payment co-signer | funding uses owner-driven sendTransaction; payment uses custody secp256k1Sign; no policy-approval on the x402 settle path | `grep -rn secp256k1Sign facilitator/` → 0; `grep createPolicy treasury/privy.ts` → ≥1 | secp256k1Sign self-broadcast on funding rail | INVARIANTS #6; PLAN Task 2.5 + 4.2 |
| F-011 | ENS is enforcement source; DB never gates a payment | the facilitator settlement path imports no DB client (reads ENS live) | `grep -rnE "db/|drizzle|DATABASE_URL|pg" facilitator/` → 0 hits | facilitator reads DB for enforcement | INVARIANTS #3; PLAN Task 2.5 |
| F-012 | Honest framing (no "trustless"/"chain enforces") | no such string on the enforcement claim in README/UI/docs/submission | `grep -rniE "trustless\|chain[ -]enforce" README.md web/ docs/ submission/` → 0 hits | "trustless"/"chain enforces the cap" appears | INVARIANTS #4; PLAN Task 6.2 step 3 (owned by deploy/demo copy) |
| F-013 | 3-level ENS hierarchy is load-bearing (not a KV store) | parent + ≥2 children with distinct caps; `getSubregistry` traversal + `revokeRoles` demoed | read `data.<org>` and `payments.<org>` policies (distinct maxPerCall); revokeRoles removes spend + write authority | flat single-name, no hierarchy/roles | ENS prize; PLAN Task 1.2/1.3 |
| F-014 | HCS audit trail records every decision | the HCS topic shows ALLOW and DENY entries for real payments | `curl mirrornode .../topics/{HCS_TOPIC_ID}/messages` → entries with decision ALLOW + DENY | topic empty OR entries fabricated | PLAN Task 2.4 |
| F-015 | Judge sandbox drives the full hero with zero setup | `/demo` runs grant→spend→refuse→revoke→DENY with no login/wallet/ETH and no import edge to `/app` | open live `/demo`, drive all 4 beats; `grep -rn "app/app" web/app/demo web/app/api/demo` → 0 | sandbox needs login/wallet OR imports /app | INVARIANTS #10; PLAN Task 5.2/5.3 (VM-2) |

**Sentinel discipline:** every observable's `sentinel_fail` is a concrete "this is fake/broken" condition. stress_test asserts the observable AND that the sentinel string is absent. No observable passes on "the endpoint returned 200" alone — each asserts a state change or a rejection code.
